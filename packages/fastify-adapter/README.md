# `@omni-queue/fastify-adapter`

Fastify integration adapter for Omni-Queue dashboard API.

For in-process mounting, ensure Fastify has middleware support (`@fastify/middie` or `@fastify/express`) and then use `registerFastifyAdapter`.
