# dynamodb-storage-lab

DynamoDB-backed storage example.

## Prerequisites

Set AWS credentials/region or point to local DynamoDB:

```bash
export AWS_REGION=us-east-1
export DYNAMODB_ENDPOINT=http://127.0.0.1:8000
```

## Run

```bash
npm install
cd examples/dynamodb-storage-lab
npm run dev
```
