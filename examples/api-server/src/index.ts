/**
 * This file is intentionally empty.
 *
 * The api-server example uses two separate entry points:
 *   - src/server.ts  — HTTP API server (run with: npm run server)
 *   - src/worker.ts  — Queue worker / supervisor (run with: npm run worker)
 *
 * See README.md for the full getting-started guide.
 */

